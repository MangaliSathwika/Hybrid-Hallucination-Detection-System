# Hybrid-Hallucination-Detection-System
Developed an interactive RAG framework via a Streamlit UI (app.py) to detect and mitigate factual errors in LLM outputs. The pipeline cross-references dynamically generated model responses against verified external vector store knowledge chunks, assessing semantic consistency to calculate real-time safety confidence scores
